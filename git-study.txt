git study

gitgit study

catcat dogdog

git init

git status

git add

git commit -m "message"

git vim -> i / esc / :wq

git cat

git remote add origin https://github.com/suyoung-h/hsy.git

git push --set-upstream origin master

git push origin master
